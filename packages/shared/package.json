{
   "name": "@repo/shared",
   "version": "1.0.0",
   "private": true,
   "main": "src/index.ts",
   "types": "src/index.ts",
   "scripts": {
      "build": "tsc -b",
      "lint": "eslint . --ext .ts",
      "clean": "rm -rf dist",
      "test": "echo \"Error: no test specified\" && exit 1"
   },
   "devDependencies": {
      "@repo/eslint-config": "*",
      "@repo/jest-config": "*",
      "@repo/typescript-config": "*",
      "@types/jest": "^29.5.12",
      "jest": "^29.7.0"
   }
}
